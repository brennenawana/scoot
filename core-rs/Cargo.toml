[package]
name = "scoot-core"
version = "0.1.0"
edition = "2021"
description = "Scoot's portable judgment core — conformant to Tests/golden (docs/CONTRACTS.md)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
